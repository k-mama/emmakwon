{
  "name": "emmakwon2",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "check:site": "node scripts/check-static-site.mjs",
    "check:security": "node scripts/check-repo-security.mjs",
    "check:functions": "tsc -p functions/tsconfig.json --noEmit",
    "prepare:studio-seed": "node scripts/generate-studio-seed.mjs",
    "backup:studio": "node scripts/backup-studio-d1.mjs",
    "verify:studio-backup": "node scripts/verify-studio-backup.mjs",
    "audit:prod": "npm audit --omit=dev --audit-level=high",
    "verify:release": "npm run check:security && npm run audit:prod && npm run check:functions && npm run lint && npm run build && npm run check:site"
  },
  "dependencies": {
    "next": "16.3.0",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260813.1",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.3.0",
    "typescript": "^5",
    "wrangler": "^4.122.0"
  }
}
